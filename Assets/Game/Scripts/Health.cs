using TMPro;
using UnityEngine;

public class interaksiNyawaSkor : MonoBehaviour
{
    public TMP_Text TeksNyawa;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
